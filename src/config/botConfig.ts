/**
 * Static bot configuration.
 *
 * Contains global configuration values shared across
 * the application, such as the command prefix and
 * other environment-independent settings.
 *
 * Dynamic or runtime-based configuration should be
 * handled separately within their respective modules.
 */
export const botConfig = {
    prefix: '---',
};